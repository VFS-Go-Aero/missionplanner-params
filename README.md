```
PRX_ALT_MIN: 5 // this is the altitude below which prx messages are ignored (should not enable avoidance though until AVOID_ALT_MIN)
AVOID_ALT_MIN: 6 // alt. at which altitude 
PRX_IGN_GND: 1 // tunring this on avoids 1m off the ground
AVOID_DIST_MAX: 10 // avoid dist for non GPS modes
AVOID_MARGIN: 10 // avoid dist for GPS modes
```
